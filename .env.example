# Vercel / serverless (private — use this name, NO REACT_APP_ prefix)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional local-only fallback when running plain `npm start` (key is exposed in browser)
# REACT_APP_GEMINI_API_KEY=
